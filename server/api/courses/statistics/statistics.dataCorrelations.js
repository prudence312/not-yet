export default function dataCorrelationsCalculator() {
  return new Promise(function(resolve, error) {
    resolve({
      description:
        'Returns connections between success on some problems and failure on others'
    });
  });
}
